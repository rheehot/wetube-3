extends layouts/main

block content
    .form-container
        form(action=`/users$/{routes.changePassword}`, method="post")
            input(type="password", name="oldPassword", placeholder="Current Password")
            input(type="password", name="newsPassword", placeholder="New Password")
            input(type="password", name="newPassword", placeholder="Verify New Password")
            input(type="submit", name="Change Password" )

